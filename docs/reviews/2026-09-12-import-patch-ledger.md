# Import-patch execution ledger

Base: da5e9345c961bcecdc8a254f7a6af3a69b412964. Existing application checkout retained; no destructive rewrite.

- Design authorization: user repeatedly directed imports for every field and now explicitly requested agents to critique and finish. Continue within this scope without asking again.
- Audit: three independent agents (UX, backend, acceptance) inspecting contracts and evidence. No Shopee requests.
- Decision: saved-source diff is local preparation; live scope/readback must stay distinct. Cost if conflated: misleading operator and possible wrong patch.
- Decision: per-shop patches overlay immutable shared source; never overwrite common product prices on import. Cost if wrong: separate explicit source editing workflow may also be needed.
- Task 1 complete in its local preparation scope: backend implementer owns domain/API/persistence/tests; final API contract at `.local/import-patch-review/backend-contract.md`. Independent spec and quality review passed; 28 targeted parser/integration tests and typecheck passed. No Shopee execution claim.
- Task 2 in progress: UI implementer owns web/browser tests only; starts from the agreed backend contract. Independent file ownership permits parallel work as requested; integration and review remain sequential gates.
- Task 3 in progress: acceptance reviewer audits the implementation independently; actual browser-to-API-to-isolated-Postgres evidence required.
- Cross-review decision: Word text changes with existing description images require explicit placement when the new headline/body boundary is unknown. Old canonical layout does not prove new Word paragraph roles. Cost: one explicit layout selection when it cannot be inferred safely from supplied mapping.
- Cross-review decision: an unresolved prior remote write blocks execution, not harmless local proposal preparation. Receipt association does not update WorkOrder or clear its recovery lock.
- Protected local baseline captured: source `lamy-5d` revision 1, existing workbench and trial metadata. `.local/import-patch-review/*-before.json` retains comparison evidence privately.
- Review findings being fixed: blank SKU with nonblank values must not disappear; imported references must belong to the selected file manifest; image/Word blobs must still exist and match their original hash/size.
- Decision: credential-only connection revision is excluded from semantic business-intent dedupe. Refreshing a token must not create a new stock instruction. Reused receipts remain historical local preparation; future execution must validate current scope/capability and cannot treat a replay as a fresh executable plan. Cost: execution integration must retain this separate validity check.
- Main local database: additive migration 009 applied after Task 1 review. Existing app restarted with its own portable runtime; `/v1/import-patches/context` returned HTTP 200 and UI listener 5173 was ready. No external API request or business write was submitted by this work.
- Task 2 cross-review blockers: a workbook block must target its explicit subset of shops/work orders; one imported image must support multiple selected roles such as gallery and description. UI implementer is addressing both, plus compact 80-SKU review and recovery after a committed save loses its response.
- Protected comparison after restart at 10:00:21 UTC: the complete products and workbench API responses match the captured baseline; Lamy stays at revision 1, no main-database patch receipt was created, worker is online, production writes remain disabled. Evidence: `.local/import-patch-review/protected-after.json` (recheck after final tests).
- Final UI review requirements: filtered group selection preserves selections outside the filter and excludes blocked/unchanged rows; adding a role on the same target preserves SKU mapping; each target/media role has independent order; description-image-only imports with unknown source layout remain blocked. The isolated browser fixture uses only its test schema in `search_path`.
- Tasks 2 and 3 complete in the preparation scope: all cross-review blockers closed. Independent spec/quality verdicts are in `docs/reviews/2026-09-12-import-patch-final-review.md` and the UI/API review. The UI implementer stopped code edits after 24/24 targeted browser checks and typecheck.
- Fresh root verification after code freeze: typecheck and builds passed; 361/361 unit/integration, 7/7 legacy, then 56/56 full browser tests passed. No code changes followed these runs. Repeated test counts overlap and must not be summed.
- Final invariant check at 10:20:27 UTC: products and workbench responses unchanged, Lamy revision 1, zero main-app patch receipts, worker online, production writes false and UI HTTP 200. Delivery: `docs/delivery/2026-09-12-import-patch-workflow.md`. Full business execution/production remains unaccepted; existing source and Shopee evidence were not rewritten.
